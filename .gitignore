# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/


# IDEs
.vscode/
.idea/

# Test Artifacts
diff_result_preview.png
# Local Claude Code state, except the skills shared with the project.
# The contents are excluded rather than the directory itself: git cannot
# re-include a path whose parent directory is excluded.
/.claude/*
!/.claude/skills/

# PyInstaller
build/
dist/
*.spec

# Vendor binaries (downloaded by scripts/download_poppler.py)
vendor/

sample-files/allianz
